- count, audit_events = audit_warnings_table
- if count == 0
  %p
    There are no warnings available
- elsif count == :forbidden
  %p.forbidden
    You do not have permission to view the warning events of this role.

- if count.is_a?(Integer) && count > 0

  = render partial: "audit_table", object: audit_events

  - obtained = @requested || audit_events.size
  - if obtained < count
    .text-center
      = audit_events_paginate("audit_warnings", @conjur_object, audit_events.size + 10) do
        %button.btn.btn-default
          Show More
